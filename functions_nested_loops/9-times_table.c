#include <stdio.h>
#include "main.h"

/**
*
*/
void times_table(void)
{
    int n, i;

    for ( n = 0; n < 10; n++)
        if ( n < 10 )

    for ( i = 0; i< 10; i++ )
        if ( i < 10 )

     _putchar("\n '%d:'\n", n);
     _putchar("\n '%d:'\n", i);
     _putchar(',');
    _putchar(' ');
}
